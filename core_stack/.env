# General
MYHOME=/home/pi/ # Update with your user's home directory
HDD_PATH=/mnt/usb0/ # The mount location of the external HDD
HDD_PATH_SHARED=${HDD_PATH}/shared/ # The location of the sharable content from the HDD
CONFIG_PATH=/home/pi/docker_compose/core_stack/ # The location of docker_appdata from the repository
PGID=1000 # pid uf user group
PUID=1000 # pid of user id
TZ=Europe/Budapest # Update to your timezone

# Basic authentication
ADMIN_USER=admin
ADMIN_PASSWORD='admin_password_hash' # Generate at https://hostingcanada.org/htpasswd-generator/
USER1=user1
USER1_PASSWORD='user1_password_hash' # Generate at https://hostingcanada.org/htpasswd-generator/

# Traefik
CERT_EMAIL=exampl@something.com # Add your valid email address
PROMETHEUS_BUCKETS=0.1,0.3,1.2,5.0 # Define the Prometheus data intervals generated by Traefik

# Authelia
AUTHELIA_TRUSTED_IPS="trusted_ips_with_subnet" # To get this value, SSH to your device, and run "ip route | grep -i eth0 | grep -v default | awk '{split($0,a," "); print a[1]}'"

# TRAEFIK HOSTS
# Update each hostname as you wish
#HOST=mydomainname.duckdns.org # created at https://www.duckdns.org/
HOST=something.mydomain.com # created at https://www.dynu.com/en-US/ControlPanel/DDNS
TRAEFIK_WHOAMI_HOST=whoami.${HOST}
TRAEFIK_HOMEPAGE_HOST=homepage.${HOST}
TRAEFIK_DEFAULT_HOST=${HOST}
TRAEFIK_TRAEFIK_HOST=traefik.${HOST}
TRAEFIK_AUTHELIA_HOST=authelia.${HOST}
TRAEFIK_PORTAINER_HOST=portainer.${HOST}

# Dynu
# Create account at # Generate at https://www.dynu.com/en-US/ControlPanel/DDNS
USERNAME=username # Dynu username
PASSWORD=password # Dynu password
HOSTNAMES=something.domain.com # created Dynu domain and subdomain
REFRESH_TIME=3600 # in seconds

